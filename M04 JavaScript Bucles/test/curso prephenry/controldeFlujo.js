function viajar(destino) {
    if (destino === 'brasil') {
        console.log('gire a la izquierda');
    } else if (destino === 'argentina') {
        console.log('gire a la derecha');
    } else {
        console.log('nos perdimos');
    }
}

viajar('brasil');
viajar('argentina')

function puedemanejar(edad) {
    si(edad >= 18) {
        console.log(verdadero);
    }
    console.log(falso)
}

puedemanejar(17);

function viajar(destino) {
    if (destino === 'Brasil') {
        console.log('Gire a la IZQUIERDA');   
    } else if (destino === 'Argentina') {
        console.log('gire a la DERECHA');
    }
}

viajar('Brasil');


Estructura if

if (condicion) {
    //instrucciones
}

var numero = 5;

if (numero == 5) {
    document.write('El numero que escogiste es 5')
}

var mostrarMensaje = true;

if (mostrarMensaje) {
    document.write('Se ejecutaran las instrucciones')
}


if (mostrarMensaje) {
    document.write('Se ejecutaran las instrucciones')
} else {
    document.write('No Se ejecutaran las instrucciones')
}

var mostrarMensaje = true;

if (mostrarMensaje) {
    document.write('Se ejecutaran las instrucciones');
} else {
    // instrucciones
}


